# Lane ACP — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dirigir um Claude (`claude-4`) por protocolo aberto ACP com a mesma supervisão que o `codex-4` já tem, e levar as 3 lanes Claude existentes a `confidence: exact` lendo o transcript que elas já escrevem.

**Architecture:** O loomd passa a ser **cliente ACP** de um `claude-agent-acp` (Node) por lane, falando ND-JSON no stdio. Um módulo novo `src/acp.rs` traduz `session/update` em `AgentEvent` e para aí — `trama.rs` não muda de contrato. A supervisão de processo filho, hoje duplicada em espírito, sai para `src/supervisao.rs`. Em paralelo, `src/transcript.rs` lê os `.jsonl` que as 3 lanes TUI já produzem, sem tocar na configuração delas.

**Tech Stack:** Rust 2021, tokio, axum 0.7, serde/serde_json. Node ≥22 para o adaptador `@agentclientprotocol/claude-agent-acp` 0.66.0.

**Spec:** [2026-08-10-lane-acp-design.md](../specs/2026-08-10-lane-acp-design.md)

## Global Constraints

- **Crate fora do workspace.** Todo comando cargo roda de `beagle/crates/loomd`. `cargo build --all` do monorepo não alcança este crate e não deve alcançar.
- **Testes inline**, em `#[cfg(test)] mod tests` no próprio arquivo. Não existe diretório `tests/` neste crate e não se cria um — 40 testes seguem esse padrão.
- **Antes de cada commit:** `cargo fmt && cargo clippy --all-targets && cargo test` (dentro de `crates/loomd`).
- **Mutação é obrigatória** em toda asserção nova: quebre a condição de propósito, veja vermelho, desfaça. Verde sem mutação não conta. Registre a mutação no corpo do commit.
- **O `codex.rs` não é reescrito.** A Task 2 move código para fora dele; qualquer outra mudança nele é fora de escopo. A suíte do codex tem de continuar verde **sem editar teste nenhum**.
- **Não tocar em `.agents/<lane>/.claude/settings.json`.** As 3 lanes estão trabalhando com `defaultMode: auto`; observação é read-only.
- **Adaptador:** `@agentclientprotocol/claude-agent-acp` **0.66.0**. O pacote `@zed-industries/claude-code-acp` está deprecado — nunca usar.
- **Enquadramento ND-JSON:** uma mensagem JSON por linha, `\n` no fim. Não há `Content-Length`.
- **Linha ilegível é registrada, nunca fatal** — regra já vigente em `codex.rs:134`.
- **Fixture do censo:** `beagle/docs/superpowers/fixtures/2026-08-10-acp-censo-modo-default.jsonl`, commitada. Os testes a alcançam pela constante `FIXTURE_ACP` definida na Task 4, relativa ao crate — por isso `cargo test` **precisa** rodar de `crates/loomd`.
- **Contagens da fixture, medidas** (rodada 2, modo `default`): 67 `session/update`, dos quais 7 `tool_call`, 7 `tool_call_update` com `status: completed`, 14 chunks de fala, 16 `usage_update`, 2 de configuração. São os números que a Task 4 asserta.

### Correção ao spec, aplicada aqui

O spec §1 propõe uma rota nova `POST /v2/lanes/:lane/decide`. **Ela não é necessária:** `main.rs:77` já registra `/v2/lanes/:lane/approve`, usada pelas aprovações do codex. A Task 6 faz a rota existente atender as duas classes de lane. Uma segunda rota com o mesmo propósito é exatamente o "dois caminhos para a mesma ação" que este projeto já pagou para aprender.

---

## File Structure

| arquivo | responsabilidade |
|---|---|
| `src/supervisao.rs` **(criar)** | Um laço: sobe filho, espera morrer, backoff, repete. Sem saber o que o filho fala. |
| `src/acp.rs` **(criar)** | Cliente ACP: transporte ND-JSON, handshake, sessão, aprovações pendentes, `prompt`/`cancel`. Emite `AgentEvent`. |
| `src/transcript.rs` **(criar)** | Lê `.jsonl` de lane TUI a partir de um offset; guardas de truncamento e rotação. |
| `src/event.rs` (modificar) | `Kind::Usage`, `ApprovalKind::from_acp_kind`, `from_acp_update`, `from_acp_permission`, `from_transcript_line`. |
| `src/codex.rs` (modificar) | Só passa a usar `supervisao::supervisionar`. Nada mais. |
| `src/main.rs` (modificar) | `LaneHandle`, sobe lanes ACP e tails, despacha as rotas. |
| `src/trama.rs` | **Não muda.** |

---

## Task 1: Medir `auto` contra `default` e fixar o modo

O spec declara isto como a primeira medição, e ela não precisa de Rust: o script do censo já está commitado. Sem este número, a Task 5 codifica um modo que pode silenciar a via de aprovação inteira.

**Files:**
- Usar: `docs/superpowers/fixtures/2026-08-10-acp-censo.mjs`
- Create: `docs/superpowers/fixtures/2026-08-10-acp-censo-modo-auto.jsonl`
- Modify: `docs/superpowers/specs/2026-08-10-lane-acp-design.md` (a seção "⚠️ `auto` e `default`")

**Interfaces:**
- Consumes: nada.
- Produces: **a decisão do modo**, uma string — `"auto"` ou `"default"` — que a Task 5 usa em `session/set_mode`. Registrada no spec e no corpo do commit.

- [ ] **Step 1: Preparar o ambiente do censo**

```bash
mkdir -p /tmp/acp-censo && cd /tmp/acp-censo
npm install --no-fund --no-audit @agentclientprotocol/claude-agent-acp@0.66.0
cp /home/devsounio/beagle/docs/superpowers/fixtures/2026-08-10-acp-censo.mjs censo.mjs
mkdir -p alvo && cd alvo && git init -q . \
  && printf 'largura = 3\naltura = 4\n' > medidas.txt \
  && git add -A && git -c user.email=d@x -c user.name=d commit -qm base
```

Se `npm install` avisar `EBADENGINE` exigindo Node ≥22, siga: foi medido funcionando em v20.19.2.

- [ ] **Step 2: Rodar em `auto` e contar os pedidos de permissão**

```bash
cd /tmp/acp-censo
ACP_MODO=auto timeout 320 node censo.mjs "$PWD/alvo" \
  "Adicione um comentario no topo de medidas.txt explicando as unidades, e rode 'wc -l medidas.txt'."
python3 - <<'PY'
import json, collections
L=[json.loads(l) for l in open("censo.jsonl")]
c=collections.Counter()
for x in L:
    if x["dir"]!="←": continue
    m=x["msg"]; meth=m.get("method")
    if meth=="session/update": c["update :: "+m["params"]["update"].get("sessionUpdate","?")]+=1
    elif meth: c["PEDIDO :: "+meth]+=1
for k,v in c.most_common(): print(f"{v:>4}  {k}")
print("\nrequest_permission em modo auto:", c["PEDIDO :: session/request_permission"])
PY
```

**Este é o número que decide.** A tarefa é a mesma da rodada 2 do censo original, que em `default` produziu **3** pedidos.

- [ ] **Step 3: Guardar a evidência**

```bash
cp /tmp/acp-censo/censo.jsonl \
   /home/devsounio/beagle/docs/superpowers/fixtures/2026-08-10-acp-censo-modo-auto.jsonl
```

- [ ] **Step 4: Escrever a decisão no spec**

Substitua o último parágrafo da seção "⚠️ `auto` e `default` não são a mesma coisa" pelo resultado real, num dos dois textos abaixo — o que corresponder ao número medido:

Se `request_permission` **> 0** em `auto`:

```markdown
**MEDIDO (Task 1 do plano):** `auto` emitiu N pedidos de `request_permission` na mesma tarefa em
que `default` emitiu 3. A via de aprovação está viva em `auto`, então o modo é **`auto`**, alinhado
ao `defaultMode: auto` das 3 lanes. Evidência em
`fixtures/2026-08-10-acp-censo-modo-auto.jsonl`.
```

Se `request_permission` **== 0** em `auto`:

```markdown
**MEDIDO (Task 1 do plano):** `auto` emitiu ZERO `request_permission` na mesma tarefa em que
`default` emitiu 3 — o classificador aprova sozinho e nada aparece na tela. O modo é **`default`**:
a lane para esperando decisão, e cada decisão perigosa fica visível no Mission Control. Isso
DIVERGE de propósito do `defaultMode: auto` das 3 lanes TUI, porque ali o operador está olhando o
terminal e aqui ele está olhando a Frota. Evidência em
`fixtures/2026-08-10-acp-censo-modo-auto.jsonl`.
```

- [ ] **Step 5: Commit**

```bash
cd /home/devsounio/beagle
git add docs/superpowers/fixtures/2026-08-10-acp-censo-modo-auto.jsonl \
        docs/superpowers/specs/2026-08-10-lane-acp-design.md
git commit -m "measure(loomd): modo ACP decidido por contagem, nao por leitura da descricao

`auto` usa classificador e podia aprovar sozinho, deixando a tela de aprovacao
sem fonte pelo mesmo mecanismo do bypassPermissions — so mais discreto.
Contado na MESMA tarefa que rendeu 3 pedidos em \`default\`."
```

---

## Task 2: Extrair a supervisão de processo filho

**Files:**
- Create: `src/supervisao.rs`
- Modify: `src/codex.rs:58-79` (o bloco `tokio::spawn` dentro de `spawn`)
- Modify: `src/main.rs:18-19` (declaração de módulos)

**Interfaces:**
- Consumes: nada.
- Produces:
  ```rust
  pub fn supervisionar<F, Fut>(lane: String, trama: Arc<Trama>, rodar: F)
  where
      F: Fn() -> Fut + Send + Sync + 'static,
      Fut: std::future::Future<Output = std::io::Result<()>> + Send;
  ```
  A Task 3 chama isto. Backoff: começa em 500 ms, dobra, teto de 15 000 ms, **volta a 500 ms** em execução bem-sucedida. Cada erro vira um `AgentEvent` de `Kind::Error`.

- [ ] **Step 1: Escrever o teste que falha**

Crie `src/supervisao.rs` com **só** o módulo de teste e a assinatura vazia:

```rust
//! O laço que mantém um filho vivo. Não sabe o que o filho fala — por isso serve ao codex
//! (JSON-RPC do app-server) e ao ACP (ND-JSON) sem virar abstração vazada.

use crate::event::{AgentEvent, Confidence, Kind};
use crate::trama::Trama;
use std::sync::Arc;

pub fn supervisionar<F, Fut>(_lane: String, _trama: Arc<Trama>, _rodar: F)
where
    F: Fn() -> Fut + Send + Sync + 'static,
    Fut: std::future::Future<Output = std::io::Result<()>> + Send,
{
}

/// Teto baixo de propósito: uma lane que não sobe tem que reaparecer no board rápido, não
/// sumir por dez minutos de backoff exponencial.
pub fn proximo_backoff(atual_ms: u64) -> u64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn backoff_dobra_e_para_no_teto() {
        assert_eq!(proximo_backoff(500), 1_000);
        assert_eq!(proximo_backoff(8_000), 15_000);
        assert_eq!(proximo_backoff(15_000), 15_000);
    }

    #[tokio::test]
    async fn falha_do_filho_vira_evento_e_o_laco_continua() {
        let dir = std::env::temp_dir().join(format!("loomd-sup-{}", std::process::id()));
        std::fs::create_dir_all(&dir).unwrap();
        let t = Arc::new(Trama::open(dir.join("trama.jsonl")));
        let n = Arc::new(std::sync::atomic::AtomicUsize::new(0));
        let n2 = n.clone();

        supervisionar("teste".into(), t.clone(), move || {
            let n = n2.clone();
            async move {
                n.fetch_add(1, std::sync::atomic::Ordering::SeqCst);
                Err(std::io::Error::new(std::io::ErrorKind::Other, "caiu de proposito"))
            }
        });

        tokio::time::sleep(std::time::Duration::from_millis(1_600)).await;

        // Reentrou pelo menos uma vez depois da primeira falha: o laço não desiste.
        assert!(n.load(std::sync::atomic::Ordering::SeqCst) >= 2);
        let erros: Vec<_> = t.since(0, Some("teste")).into_iter()
            .filter(|e| e.kind == Kind::Error)
            .collect();
        assert!(!erros.is_empty(), "a falha do filho tem de aparecer na trama");
        assert!(erros[0].detail.as_deref().unwrap().contains("caiu de proposito"));
        std::fs::remove_dir_all(&dir).ok();
    }
}
```

Declare o módulo em `src/main.rs`, na lista que hoje é `mod codex; mod event; mod trama;`:

```rust
mod supervisao;
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test supervisao 2>&1 | tail -20
```

Esperado: FAIL — `backoff_dobra_e_para_no_teto` com `assertion failed: 0 == 1000`, e `falha_do_filho…` com o `assert!(n >= 2)`.

- [ ] **Step 3: Implementar**

Substitua as duas funções vazias:

```rust
pub fn proximo_backoff(atual_ms: u64) -> u64 {
    (atual_ms * 2).min(15_000)
}

pub fn supervisionar<F, Fut>(lane: String, trama: Arc<Trama>, rodar: F)
where
    F: Fn() -> Fut + Send + Sync + 'static,
    Fut: std::future::Future<Output = std::io::Result<()>> + Send,
{
    tokio::spawn(async move {
        let mut backoff_ms = 500u64;
        loop {
            match rodar().await {
                // Subida boa zera o castigo: uma lane que ficou horas de pé e caiu uma vez
                // merece voltar em meio segundo, não no teto herdado de falhas antigas.
                Ok(()) => backoff_ms = 500,
                Err(e) => {
                    trama.append(
                        AgentEvent::new(&lane, Kind::Error, Confidence::Exact)
                            .detail(format!("filho caiu: {e}")),
                    );
                }
            }
            tokio::time::sleep(std::time::Duration::from_millis(backoff_ms)).await;
            backoff_ms = proximo_backoff(backoff_ms);
        }
    });
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test supervisao 2>&1 | grep -E "^test result|FAILED"
```

Esperado: `test result: ok. 2 passed`.

- [ ] **Step 5: Mutação — provar que os testes exercem a condição**

```bash
cd /home/devsounio/beagle/crates/loomd
cp src/supervisao.rs /tmp/sup.bak
# Mutação A: teto some
sed -i 's/(atual_ms \* 2).min(15_000)/atual_ms * 2/' src/supervisao.rs
cargo test supervisao 2>&1 | grep -E "^test result"     # PRECISA falhar
cp /tmp/sup.bak src/supervisao.rs
# Mutação B: o erro não vira evento
python3 - <<'PY'
p="src/supervisao.rs"; s=open(p).read()
a='''                Err(e) => {
                    trama.append('''
assert a in s
s=s.replace(a,'''                Err(_e) => {
                    let _ = |x: AgentEvent| x; drop(''',1)
open(p,"w").write(s)
PY
cargo test supervisao 2>&1 | grep -E "^test result|error\[" | head -3   # PRECISA falhar (ou nem compilar)
cp /tmp/sup.bak src/supervisao.rs
cargo test supervisao 2>&1 | grep -E "^test result"     # volta a verde
```

Se qualquer mutação ficar **verde**, o teste é teatral — conserte o teste antes de seguir.

- [ ] **Step 6: `codex.rs` passa a usar o laço, sem editar teste nenhum**

Em `src/codex.rs`, troque o bloco `tokio::spawn` de `spawn()` (linhas 58-77) por:

```rust
        let (l, b, c, a) = (lane.to_string(), bin.to_string(), cwd.to_string(), args);
        let this = me.clone();
        crate::supervisao::supervisionar(l, me.trama.clone(), move || {
            let (this, b, c, a) = (this.clone(), b.clone(), c.clone(), a.clone());
            async move { this.run_once(&b, &c, &a).await }
        });
```

- [ ] **Step 7: A suíte inteira, sem tocar em teste**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo fmt && cargo clippy --all-targets 2>&1 | grep -E "^(warning|error)" | head
cargo test 2>&1 | grep -E "^test result|FAILED"
```

Esperado: todas verdes. **Se um teste do codex precisar de edição, pare** — a extração passou de mover para redesenhar, e isso é fora de escopo.

- [ ] **Step 8: Commit**

```bash
cd /home/devsounio/beagle
git add crates/loomd/src/supervisao.rs crates/loomd/src/codex.rs crates/loomd/src/main.rs
git commit -m "refactor(loomd): laco de supervisao sai para src/supervisao.rs

Vai existir um segundo supervisor de filho (a lane ACP). Extraido AGORA, com dois
usos reais, e nao antes. O codex.rs so passou a chamar: nenhum teste dele foi
editado, o que e a prova de que foi mover e nao redesenhar.

Mutacoes: teto do backoff removido -> vermelho; erro nao virando evento -> vermelho."
```

---

## Task 3: Transporte ND-JSON e handshake

**Files:**
- Create: `src/acp.rs`
- Modify: `src/main.rs` (declarar `mod acp;`)

**Interfaces:**
- Consumes: `supervisao::supervisionar` (Task 2).
- Produces:
  ```rust
  pub struct AcpLane { pub lane: String, /* privados */ }
  impl AcpLane {
      pub fn spawn(lane: &str, bin: &str, cwd: &str, modo: &str, trama: Arc<Trama>) -> Arc<Self>;
      pub async fn session_id(&self) -> Option<String>;
  }
  pub fn linhas_ndjson(bruto: &str) -> Vec<serde_json::Value>;
  ```
  `modo` é a string decidida na Task 1. As Tasks 4-6 estendem este `impl`.

- [ ] **Step 1: Escrever o teste que falha**

Crie `src/acp.rs`:

```rust
//! Cliente ACP — Agent Client Protocol.
//!
//! 🚨 A INVERSÃO IMPORTA: o adaptador (`claude-agent-acp`, Node) é o SERVIDOR; o loomd é o
//! CLIENTE. É o papel que o Zed ocupa, e foi ele que motivou trocar `stream-json` por protocolo
//! aberto: ACP é UMA tradução para N agentes (Codex e Gemini também têm adaptador), enquanto
//! `stream-json` seria o segundo dialeto artesanal deste crate.
//!
//! Enquadramento: ND-JSON, uma mensagem por linha. Não há `Content-Length`.

use crate::event::{AgentEvent, Confidence, Kind};
use crate::trama::Trama;
use std::collections::HashMap;
use std::process::Stdio;
use std::sync::Arc;
use tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader};
use tokio::sync::{oneshot, Mutex};

const ID_INIT: u64 = 1;
const ID_SESSAO: u64 = 2;
const ID_MODO: u64 = 3;

pub struct AcpLane {
    pub lane: String,
    stdin: Arc<Mutex<Option<tokio::process::ChildStdin>>>,
    /// Aprovações penduradas: id JSON-RPC → quem espera a decisão do operador.
    pending: Arc<Mutex<HashMap<String, oneshot::Sender<String>>>>,
    sessao: Arc<Mutex<Option<String>>>,
    proximo_id: Arc<Mutex<u64>>,
    trama: Arc<Trama>,
    modo: String,
    cwd: String,
}

/// Parte um bloco cru em mensagens. Linha vazia é ignorada; linha ilegível é DESCARTADA e não
/// derruba nada — regra já vigente no `codex.rs`, porque o vocabulário cresce e o transporte
/// pode intercalar ruído. Cegueira total é pior que informação parcial.
pub fn linhas_ndjson(bruto: &str) -> Vec<serde_json::Value> {
    Vec::new()
}

/// As mensagens de abertura, em ordem. **Função pura de propósito**: é o único jeito de assertar
/// que o `session/set_mode` está lá sem subir um Node. Ver o teste da mutação 1.
pub fn mensagens_de_abertura(
    sessao: Option<&str>,
    modo: &str,
    cwd: &str,
) -> Vec<serde_json::Value> {
    Vec::new()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn ndjson_parte_por_linha_e_ignora_ruido() {
        let bruto = "{\"a\":1}\n\n nao e json \n{\"b\":2}\n";
        let v = linhas_ndjson(bruto);
        assert_eq!(v.len(), 2, "duas mensagens legiveis, o ruido nao conta e nao mata");
        assert_eq!(v[0]["a"], 1);
        assert_eq!(v[1]["b"], 2);
    }

    #[test]
    fn ndjson_vazio_nao_produz_nada() {
        assert!(linhas_ndjson("").is_empty());
        assert!(linhas_ndjson("\n\n\n").is_empty());
    }

    /// 🚨 A MUTAÇÃO 1 DO SPEC, fechada por teste unitário e não só pela prova ao vivo.
    ///
    /// O adaptador nasce em `bypassPermissions`; nesse modo `session/request_permission` não
    /// dispara nenhuma vez. Se alguém remover o `set_mode`, TODA a tabela de aprovação vira
    /// código morto e a única evidência seria uma tela vazia — que se lê como "o ACP não pede
    /// permissão". O handshake é função PURA justamente para que isso seja assertável.
    #[test]
    fn o_handshake_manda_set_mode_e_ele_e_o_ultimo() {
        let ms = mensagens_de_abertura(Some("sess-1"), "default", "/workspace/.wt/claude-4");
        let metodos: Vec<&str> = ms
            .iter()
            .map(|m| m["method"].as_str().unwrap_or(""))
            .collect();
        assert_eq!(metodos, vec!["initialize", "session/load", "session/set_mode"]);
        let sm = ms.last().unwrap();
        assert_eq!(sm["params"]["modeId"], "default");
        assert_eq!(sm["params"]["sessionId"], "sess-1");
    }

    #[test]
    fn sem_sessao_conhecida_o_handshake_abre_uma_nova() {
        let ms = mensagens_de_abertura(None, "auto", "/workspace/.wt/claude-4");
        let metodos: Vec<&str> = ms.iter().map(|m| m["method"].as_str().unwrap_or("")).collect();
        // Sem sessão ainda não há `sessionId` para o `set_mode`: ele vai quando a resposta
        // do `session/new` chegar. Duas mensagens aqui, e a segunda é `session/new`.
        assert_eq!(metodos, vec!["initialize", "session/new"]);
        assert_eq!(ms[1]["params"]["cwd"], "/workspace/.wt/claude-4");
    }

    #[test]
    fn fs_e_declarado_falso_porque_o_agente_nunca_chama() {
        let ms = mensagens_de_abertura(None, "default", "/tmp");
        let caps = &ms[0]["params"]["clientCapabilities"]["fs"];
        assert_eq!(caps["readTextFile"], false);
        assert_eq!(caps["writeTextFile"], false);
    }
}
```

Declare em `src/main.rs`: `mod acp;`

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test acp:: 2>&1 | tail -20
```

Esperado: **quatro** vermelhas — `ndjson_parte_por_linha_e_ignora_ruido` (`0 == 2`), `o_handshake_manda_set_mode_e_ele_e_o_ultimo`, `sem_sessao_conhecida_o_handshake_abre_uma_nova` e `fs_e_declarado_falso_porque_o_agente_nunca_chama` (todas com vetor vazio). `ndjson_vazio_nao_produz_nada` passa desde já — é a única que a implementação vazia satisfaz, e isso é esperado.

- [ ] **Step 3: Implementar o parser e o handshake**

```rust
pub fn linhas_ndjson(bruto: &str) -> Vec<serde_json::Value> {
    bruto
        .lines()
        .map(str::trim)
        .filter(|l| !l.is_empty())
        .filter_map(|l| serde_json::from_str(l).ok())
        .collect()
}

pub fn mensagens_de_abertura(
    sessao: Option<&str>,
    modo: &str,
    cwd: &str,
) -> Vec<serde_json::Value> {
    // `fs/*` FALSO de propósito: medido no censo que o agente nunca chamou `fs/read_text_file`
    // — ele usa a ferramenta `Read` dele. A capacidade serve a editor com buffer sujo, que não é
    // o nosso caso, e prometer o que não se usa é dívida.
    let mut v = vec![serde_json::json!({
        "jsonrpc": "2.0", "id": ID_INIT, "method": "initialize",
        "params": {
            "protocolVersion": 1,
            "clientCapabilities": {"fs": {"readTextFile": false, "writeTextFile": false}}
        }
    })];
    match sessao {
        Some(sid) => {
            v.push(serde_json::json!({
                "jsonrpc":"2.0","id":ID_SESSAO,"method":"session/load",
                "params":{"sessionId": sid, "cwd": cwd, "mcpServers": []}
            }));
            // Sessão conhecida: o modo já pode ir junto. Sem sessão, ele vai quando a resposta
            // do `session/new` trouxer o id — em `on_message`.
            v.push(serde_json::json!({
                "jsonrpc":"2.0","id":ID_MODO,"method":"session/set_mode",
                "params":{"sessionId": sid, "modeId": modo}
            }));
        }
        None => v.push(serde_json::json!({
            "jsonrpc":"2.0","id":ID_SESSAO,"method":"session/new",
            "params":{"cwd": cwd, "mcpServers": []}
        })),
    }
    v
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test acp:: 2>&1 | grep -E "^test result"
```

Esperado: `5 passed`.

- [ ] **Step 4b: Mutação — a que protege a via de aprovação inteira**

```bash
cd /home/devsounio/beagle/crates/loomd
cp src/acp.rs /tmp/acp.bak
# Mutação: o set_mode desaparece do handshake (o cenário real: alguém "simplifica")
python3 - <<'PY'
p="src/acp.rs"; s=open(p).read()
a='''            v.push(serde_json::json!({
                "jsonrpc":"2.0","id":ID_MODO,"method":"session/set_mode",'''
assert a in s, "ancora do set_mode"
s=s.replace(a,'''            let _ = ID_MODO;
            #[allow(unreachable_code)]
            if false { v.push(serde_json::json!({
                "jsonrpc":"2.0","id":ID_MODO,"method":"session/set_mode",''',1)
a2='''                "params":{"sessionId": sid, "modeId": modo}
            }));'''
assert a2 in s
s=s.replace(a2,'''                "params":{"sessionId": sid, "modeId": modo}
            })); }''',1)
open(p,"w").write(s)
PY
cargo test o_handshake_manda_set_mode 2>&1 | grep -E "^test result|error\[" | head -3   # PRECISA falhar
cp /tmp/acp.bak src/acp.rs
# Mutação: parser de ND-JSON deixa de tolerar ruído (derruba a lane por uma linha suja)
python3 - <<'PY'
p="src/acp.rs"; s=open(p).read()
a=".filter_map(|l| serde_json::from_str(l).ok())"
assert a in s
s=s.replace(a,".map(|l| serde_json::from_str(l).unwrap_or(serde_json::Value::Null))",1)
open(p,"w").write(s)
PY
cargo test ndjson_parte 2>&1 | grep -E "^test result"   # PRECISA falhar (3 em vez de 2)
cp /tmp/acp.bak src/acp.rs
cargo test acp:: 2>&1 | grep -E "^test result"
```

- [ ] **Step 5: Implementar `spawn` e `run_once`**

Acrescente ao `impl AcpLane` (antes do `mod tests`):

```rust
impl AcpLane {
    pub fn spawn(lane: &str, bin: &str, cwd: &str, modo: &str, trama: Arc<Trama>) -> Arc<Self> {
        // A sessão vem da TRAMA na subida fria, igual ao codex: o daemon é descartável, a
        // sessão não. `session/load` é capacidade anunciada (`loadSession: true`), então
        // retomar é chamada de protocolo e não heurística.
        let s0 = trama.session_of(lane);
        if let Some(s) = &s0 {
            eprintln!("[loomd] lane {lane}: sessao ACP anotada na trama, vou carregar: {s}");
        }
        let me = Arc::new(Self {
            lane: lane.to_string(),
            stdin: Arc::new(Mutex::new(None)),
            pending: Arc::new(Mutex::new(HashMap::new())),
            sessao: Arc::new(Mutex::new(s0)),
            proximo_id: Arc::new(Mutex::new(10)),
            trama: trama.clone(),
            modo: modo.to_string(),
            cwd: cwd.to_string(),
        });
        let (b, c) = (bin.to_string(), cwd.to_string());
        let this = me.clone();
        crate::supervisao::supervisionar(lane.to_string(), trama, move || {
            let (this, b, c) = (this.clone(), b.clone(), c.clone());
            async move { this.run_once(&b, &c).await }
        });
        me
    }

    pub async fn session_id(&self) -> Option<String> {
        self.sessao.lock().await.clone()
    }

    async fn enviar(&self, v: serde_json::Value) {
        let mut g = self.stdin.lock().await;
        if let Some(si) = g.as_mut() {
            let _ = si.write_all(format!("{v}\n").as_bytes()).await;
            let _ = si.flush().await;
        }
    }

    async fn run_once(&self, bin: &str, cwd: &str) -> std::io::Result<()> {
        let mut child = tokio::process::Command::new(bin)
            .current_dir(cwd)
            .stdin(Stdio::piped())
            .stdout(Stdio::piped())
            .stderr(Stdio::null())
            .kill_on_drop(true)
            .spawn()?;
        let out = child.stdout.take().expect("stdout pipe");
        *self.stdin.lock().await = child.stdin.take();

        // O handshake vem da função PURA — é ela que o teste da mutação 1 asserta. Montar as
        // mensagens aqui dentro tornaria o `set_mode` inassertável sem subir um Node.
        let sid0 = self.sessao.lock().await.clone();
        for m in mensagens_de_abertura(sid0.as_deref(), &self.modo, cwd) {
            self.enviar(m).await;
        }

        let mut lines = BufReader::new(out).lines();
        while let Some(line) = lines.next_line().await? {
            if line.trim().is_empty() {
                continue;
            }
            if std::env::var("LOOMD_DEBUG").is_ok() {
                eprintln!("[acp rx] {}", &line.chars().take(220).collect::<String>());
            }
            let Ok(m) = serde_json::from_str::<serde_json::Value>(&line) else {
                continue;
            };
            self.on_message(m).await;
        }

        let _ = child.wait().await;
        *self.stdin.lock().await = None;
        self.cancelar_pendentes("adaptador ACP encerrou").await;
        self.trama.append(
            AgentEvent::new(&self.lane, Kind::SessionEnded, Confidence::Exact)
                .detail("adaptador ACP encerrou"),
        );
        Ok(())
    }

    async fn on_message(&self, m: serde_json::Value) {
        // Resposta ao `session/new` / `session/load`: guarda a sessão e FIXA O MODO.
        if m.get("id").and_then(|x| x.as_u64()) == Some(ID_SESSAO) {
            if let Some(sid) = m.pointer("/result/sessionId").and_then(|x| x.as_str()) {
                *self.sessao.lock().await = Some(sid.to_string());
                let mut e = AgentEvent::new(&self.lane, Kind::SessionStarted, Confidence::Exact)
                    .detail(format!("sessao ACP {sid}"));
                e.session = Some(sid.to_string());
                self.trama.append(e);
                self.fixar_modo(sid).await;
            }
            return;
        }
        let _ = self.traduzir(&m).await;
    }

    /// 🚨 SEM ISTO A TABELA DE APROVAÇÃO É CÓDIGO MORTO.
    ///
    /// Medido: `session/new` devolve `currentModeId: "bypassPermissions"`, e nesse modo o
    /// `session/request_permission` NÃO DISPARA UMA VEZ — o próprio SDK avisa no stderr que
    /// `canUseTool` fica sombreado. Escrito por dedução, a tela de aprovação ficaria vazia e a
    /// conclusão seria "o ACP não emite permissão".
    async fn fixar_modo(&self, sid: &str) {
        self.enviar(serde_json::json!({
            "jsonrpc":"2.0","id":ID_MODO,"method":"session/set_mode",
            "params":{"sessionId": sid, "modeId": self.modo}
        }))
        .await;
    }

    /// Preenchida na Task 4.
    async fn traduzir(&self, _m: &serde_json::Value) -> Option<()> {
        None
    }

    /// Preenchida na Task 5.
    async fn cancelar_pendentes(&self, _motivo: &str) {}
}
```

- [ ] **Step 6: Compilar e rodar a suíte**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo fmt && cargo clippy --all-targets 2>&1 | grep -E "^error" | head
cargo test 2>&1 | grep -E "^test result|FAILED"
```

Esperado: compila, tudo verde.

- [ ] **Step 7: Commit**

```bash
cd /home/devsounio/beagle
git add crates/loomd/src/acp.rs crates/loomd/src/main.rs
git commit -m "feat(loomd): cliente ACP — transporte ND-JSON, handshake e set_mode explicito

O loomd e o CLIENTE; o adaptador Node e o servidor. `fs/*` declarado FALSO porque
o censo mostrou que o agente nunca chama `fs/read_text_file` — usa a ferramenta
Read dele.

set_mode entra AQUI e nao depois: o adaptador nasce em bypassPermissions e nesse
modo request_permission nao dispara nenhuma vez.

Mutacao: parser de ND-JSON devolvendo Vec::new() -> vermelho."
```

---

## Task 4: A tabela de tradução, com replay do fio real

**Files:**
- Modify: `src/event.rs` (`Kind::Usage`, `from_acp_update`)
- Modify: `src/acp.rs` (`traduzir`)

**Interfaces:**
- Consumes: `AcpLane::traduzir` (Task 3).
- Produces:
  ```rust
  // event.rs
  pub enum Kind { /* … */ Usage }
  /// `None` = evento reconhecido que NÃO vai para a trama (delta, refino de título).
  pub fn from_acp_update(lane: &str, update: &serde_json::Value) -> Option<AgentEvent>;
  pub const FIXTURE_ACP: &str = "../../docs/superpowers/fixtures/2026-08-10-acp-censo-modo-default.jsonl";
  ```

- [ ] **Step 1: Escrever o teste que falha**

Em `src/event.rs`, dentro do `#[cfg(test)] mod tests` existente:

```rust
    /// O REPLAY DOURADO: 70 linhas de fio REAL, capturadas com a credencial do operador numa
    /// tarefa de verdade (ler, escrever, rodar shell). Rede de regressão feita de dado medido —
    /// não de exemplo que eu inventei e que por isso concorda comigo por construção.
    #[test]
    fn replay_do_censo_acp_produz_a_trama_esperada() {
        let bruto = std::fs::read_to_string(FIXTURE_ACP)
            .expect("fixture do censo ACP — rode de dentro de crates/loomd");

        let mut fala = 0;
        let mut abriu = 0;
        let mut fechou = 0;
        let mut custo = 0;
        let mut descartados = 0;

        for l in bruto.lines().filter(|l| !l.trim().is_empty()) {
            let env: serde_json::Value = serde_json::from_str(l).unwrap();
            if env["dir"] != "\u{2190}" {
                continue; // só o que o AGENTE mandou
            }
            let m = &env["msg"];
            if m["method"] != "session/update" {
                continue;
            }
            match from_acp_update("claude-4", &m["params"]["update"]) {
                None => descartados += 1,
                Some(e) => match e.kind {
                    Kind::AgentMessage => fala += 1,
                    Kind::ToolCall => abriu += 1,
                    Kind::ToolResult => fechou += 1,
                    Kind::Usage => custo += 1,
                    _ => {}
                },
            }
        }

        // Contagens do censo, rodada 2 (modo default): 7 tool_call, 28 tool_call_update dos
        // quais 7 fecham, 14 chunks de fala, 16 usage.
        assert_eq!(abriu, 7, "toda ferramenta que abriu tem de virar ToolCall");
        assert_eq!(fechou, 7, "so `status: completed` fecha — as outras 21 sao refino");
        assert_eq!(custo, 16, "usage_update entrega custo por turno de graca");
        assert_eq!(fala, 0, "chunk de fala NAO vai para a trama: coalesce em RAM");
        assert_eq!(descartados, 37, "14 chunks + 21 refinos + 2 de config, medido na fixture");
    }

    #[test]
    fn tool_call_update_sem_status_nao_persiste() {
        let refino = serde_json::json!({
            "sessionUpdate": "tool_call_update",
            "toolCallId": "toolu_1",
            "title": "Edit medidas.txt",
            "kind": "edit"
        });
        assert!(
            from_acp_update("claude-4", &refino).is_none(),
            "refino de titulo nao e evento — sao 5 mensagens por ferramenta, so 2 interessam"
        );
    }

    #[test]
    fn variante_desconhecida_vira_unknown_e_nao_desaparece() {
        let novo = serde_json::json!({"sessionUpdate": "coisa_que_nao_existia_ontem"});
        let e = from_acp_update("claude-4", &novo).expect("desconhecido e informacao");
        assert_eq!(e.kind, Kind::Unknown);
        assert!(e.detail.unwrap().contains("coisa_que_nao_existia_ontem"));
    }
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test replay_do_censo 2>&1 | tail -15
```

Esperado: FAIL — `cannot find function from_acp_update` / `no variant named Usage`.

- [ ] **Step 3: Implementar**

Em `src/event.rs`, acrescente `Usage` ao enum `Kind` (depois de `Idle`, antes de `Error`):

```rust
    /// Custo e janela de contexto do turno. Chega sem ser pedido (`usage_update`) e não existia
    /// em nenhum outro lugar da plataforma. Escopo aqui é persistir e expor por `/turns`;
    /// desenhar na tela é outra fatia.
    Usage,
```

E, no fim do arquivo:

```rust
pub const FIXTURE_ACP: &str =
    "../../docs/superpowers/fixtures/2026-08-10-acp-censo-modo-default.jsonl";

impl ApprovalKind {
    /// O ACP declara `toolCall.kind` — `edit` é reversível por git, `execute` não é. A
    /// distinção não é cosmética: ela muda o rótulo do botão.
    pub fn from_acp_kind(kind: &str) -> Self {
        match kind {
            "edit" => ApprovalKind::Patch,
            "execute" => ApprovalKind::Command,
            _ => ApprovalKind::Other,
        }
    }
}

/// Traduz uma `session/update` do ACP.
///
/// `None` significa **reconhecido e deliberadamente não persistido** — nunca "não entendi".
/// O que não se entende vira `Kind::Unknown`, porque o vocabulário cresce a cada versão e um
/// evento novo tem de aparecer no board, não sumir.
pub fn from_acp_update(lane: &str, u: &serde_json::Value) -> Option<AgentEvent> {
    let variante = u.get("sessionUpdate").and_then(|x| x.as_str()).unwrap_or("");
    let texto = |campo: &str| -> String {
        u.pointer(&format!("/{campo}/text"))
            .and_then(|x| x.as_str())
            .unwrap_or_default()
            .to_string()
    };
    match variante {
        // Deltas de fala e de pensamento coalescem em RAM (`Trama::acumular_delta`) e não
        // viram linha na trama: 30% do fluxo do ACP, e persistir cada pedaço encheria o
        // arquivo de fragmentos que ninguém lê.
        "agent_message_chunk" | "agent_thought_chunk" => None,
        "user_message_chunk" => {
            Some(AgentEvent::new(lane, Kind::UserPrompt, Confidence::Exact).with_text(texto("content")))
        }
        // Abre `pending`. Quem fecha é `tool_call_update` com `status: completed`.
        "tool_call" => {
            let mut e = AgentEvent::new(lane, Kind::ToolCall, Confidence::Exact)
                .with_text(u.get("title").and_then(|x| x.as_str()).unwrap_or("").to_string());
            e.tool = u.get("kind").and_then(|x| x.as_str()).map(str::to_string);
            Some(e)
        }
        "tool_call_update" => {
            // 🚨 5 mensagens por ferramenta, medido (35 para 7). Título refina, `content`
            // chega, e SÓ ENTÃO vem `status: completed` com `rawOutput`. Persistir todas
            // multiplicaria a trama por 2,5 sem acrescentar um fato.
            if u.get("status").and_then(|x| x.as_str()) != Some("completed") {
                return None;
            }
            let saida = u
                .get("rawOutput")
                .map(|x| x.as_str().map(str::to_string).unwrap_or_else(|| x.to_string()))
                .unwrap_or_default();
            let mut e = AgentEvent::new(lane, Kind::ToolResult, Confidence::Exact).with_text(saida);
            e.tool = u
                .pointer("/_meta/claudeCode/toolName")
                .and_then(|x| x.as_str())
                .map(str::to_string);
            Some(e)
        }
        "usage_update" => {
            let usados = u.get("used").and_then(|x| x.as_u64()).unwrap_or(0);
            let teto = u.get("size").and_then(|x| x.as_u64()).unwrap_or(0);
            let custo = u.pointer("/cost/amount").and_then(|x| x.as_f64()).unwrap_or(0.0);
            Some(
                AgentEvent::new(lane, Kind::Usage, Confidence::Exact)
                    .detail(format!("contexto {usados}/{teto} · USD {custo:.4}")),
            )
        }
        // Registrados sem UI: não há tela de plano, e prometer uma aqui seria escopo que o
        // spec explicitamente não pega.
        "plan" | "plan_update" | "plan_removed" => None,
        "available_commands_update"
        | "config_option_update"
        | "current_mode_update"
        | "session_info_update" => None,
        outro => Some(
            AgentEvent::new(lane, Kind::Unknown, Confidence::Exact)
                .detail(format!("session/update desconhecida: {outro}")),
        ),
    }
}
```

Em `src/acp.rs`, ligue a tradução e a coalescência:

```rust
    async fn traduzir(&self, m: &serde_json::Value) -> Option<()> {
        if m.get("method").and_then(|x| x.as_str()) != Some("session/update") {
            return None;
        }
        let u = m.pointer("/params/update")?;
        let variante = u.get("sessionUpdate").and_then(|x| x.as_str()).unwrap_or("");
        if variante == "agent_message_chunk" || variante == "agent_thought_chunk" {
            if let Some(t) = u.pointer("/content/text").and_then(|x| x.as_str()) {
                self.trama.acumular_delta(&self.lane, t);
            }
            return Some(());
        }
        // Fala completa: o delta acumulado já foi entregue por partes, então limpa antes de
        // registrar o evento inteiro — senão o próximo turno herda texto do anterior.
        if variante == "tool_call" {
            self.trama.limpar_delta(&self.lane);
        }
        if let Some(mut e) = crate::event::from_acp_update(&self.lane, u) {
            e.session = self.sessao.lock().await.clone();
            self.trama.append(e);
        }
        Some(())
    }
```

- [ ] **Step 4: Rodar e ver passar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test from_acp 2>&1 | grep -E "^test result"
cargo test replay_do_censo -- --nocapture 2>&1 | grep -E "^test result|panicked"
```

Esperado: todas verdes. Se as contagens não fecharem, **acredite na fixture** e conserte o tradutor — o arquivo é o fio real.

- [ ] **Step 5: Mutação**

```bash
cd /home/devsounio/beagle/crates/loomd
cp src/event.rs /tmp/ev.bak
# Mutação A: refino de ferramenta passa a persistir
python3 - <<'PY'
p="src/event.rs"; s=open(p).read()
a='            if u.get("status").and_then(|x| x.as_str()) != Some("completed") {\n                return None;\n            }'
assert a in s, "ancora da guarda de status"
s=s.replace(a,'            // mutacao',1); open(p,"w").write(s)
PY
cargo test replay_do_censo 2>&1 | grep -E "^test result"   # PRECISA falhar (fechou = 28)
cp /tmp/ev.bak src/event.rs
# Mutação B: variante desconhecida passa a ser engolida
python3 - <<'PY'
p="src/event.rs"; s=open(p).read()
a='        outro => Some(\n            AgentEvent::new(lane, Kind::Unknown, Confidence::Exact)'
assert a in s
s=s.replace(a,'        _outro => None::<AgentEvent>.map(\n            |_e: AgentEvent| AgentEvent::new(lane, Kind::Unknown, Confidence::Exact)',1)
open(p,"w").write(s)
PY
cargo test variante_desconhecida 2>&1 | grep -E "^test result|error\[" | head -3   # PRECISA falhar
cp /tmp/ev.bak src/event.rs
cargo test 2>&1 | grep -E "^test result"
```

- [ ] **Step 6: Commit**

```bash
cd /home/devsounio/beagle
git add crates/loomd/src/event.rs crates/loomd/src/acp.rs
git commit -m "feat(loomd): tabela de traducao ACP + replay dourado do fio real

A tabela vem DECLARADA pelo schema (13 variantes de session/update em 262
definicoes), diferente do codex.rs que foi derivado de censo cru.

`tool_call_update` sem `status` nao persiste: 5 mensagens por ferramenta medidas,
so 2 sao fato. Kind::Usage entra porque `usage_update` entrega custo por turno
sem ser pedido.

Teste come as 70 linhas capturadas com credencial real — nao exemplo inventado,
que concorda comigo por construcao.

Mutacoes: guarda de `status` removida -> 28 em vez de 7, vermelho; desconhecido
engolido -> vermelho."
```

---

## Task 5: Aprovação pendurada — e o vazamento

**Files:**
- Modify: `src/acp.rs` (`on_message`, `cancelar_pendentes`, novo `answer`)
- Modify: `src/event.rs` (`from_acp_permission`)

**Interfaces:**
- Consumes: `from_acp_update` (Task 4).
- Produces:
  ```rust
  // event.rs
  pub fn from_acp_permission(lane: &str, id: &str, params: &serde_json::Value) -> AgentEvent;
  pub fn acp_option_id(params: &serde_json::Value, allow: bool) -> Option<String>;
  // acp.rs
  impl AcpLane { pub async fn answer(&self, approval_id: &str, allow: bool) -> Result<(), String>; }
  ```

- [ ] **Step 1: Escrever os testes que falham**

Em `src/event.rs`, no `mod tests`:

```rust
    /// O pedido REAL, extraído da fixture. Escrever isto à mão seria escrever o que eu acho que
    /// o protocolo manda — e foi exatamente assim que eu errei o `codex.rs` duas vezes.
    fn pedido_real_da_fixture() -> serde_json::Value {
        let bruto = std::fs::read_to_string(FIXTURE_ACP).expect("fixture");
        for l in bruto.lines().filter(|l| !l.trim().is_empty()) {
            let env: serde_json::Value = serde_json::from_str(l).unwrap();
            if env["msg"]["method"] == "session/request_permission" {
                return env["msg"]["params"].clone();
            }
        }
        panic!("a fixture tem de conter request_permission — foi medido em modo default");
    }

    #[test]
    fn permissao_de_edicao_vira_patch_com_o_diff_pronto() {
        let p = pedido_real_da_fixture();
        let e = from_acp_permission("claude-4", "42", &p);
        assert_eq!(e.kind, Kind::AwaitingApproval);
        assert_eq!(e.approval_kind, Some(ApprovalKind::Patch));
        assert_eq!(e.approval_id.as_deref(), Some("42"));
        let d = e.diff.expect("o ACP entrega o diff PRONTO — nao se calcula nada aqui");
        assert!(d.contains("medidas.txt"));
        assert!(d.contains("largura = 3"));
    }

    /// 🚨 `options[0]` é `reject_once` no que foi medido. Escolher por posição NEGA TUDO —
    /// silenciosamente, e com a tela dizendo que aprovou.
    #[test]
    fn a_opcao_sai_do_kind_declarado_nunca_da_posicao() {
        let p = pedido_real_da_fixture();
        assert_eq!(p["options"][0]["kind"], "reject_once", "premissa do teste, da fixture");
        assert_eq!(acp_option_id(&p, true).as_deref(), Some("allow_always"));
        assert_eq!(acp_option_id(&p, false).as_deref(), Some("reject"));
    }
```

Em `src/acp.rs`, no `mod tests`:

```rust
    /// O VAZAMENTO. É o único ponto deste desenho em que a falha produz UI MENTIROSA em vez de
    /// UI ausente: um botão na tela do Mission Control que não responde a ninguém, para sempre.
    #[tokio::test]
    async fn morte_do_filho_resolve_toda_aprovacao_pendente() {
        let dir = std::env::temp_dir().join(format!("loomd-acp-{}", std::process::id()));
        std::fs::create_dir_all(&dir).unwrap();
        let t = Arc::new(Trama::open(dir.join("trama.jsonl")));
        let lane = AcpLane::nua_para_teste("claude-4", t.clone(), "default", None);

        let (tx, mut rx) = oneshot::channel::<String>();
        lane.pending.lock().await.insert("77".into(), tx);

        lane.cancelar_pendentes("adaptador morreu").await;

        assert!(lane.pending.lock().await.is_empty(), "nenhuma pendencia pode sobrar");
        assert!(rx.try_recv().is_ok(), "quem esperava a decisao tem de ser liberado");
        let resp: Vec<_> = t.since(0, Some("claude-4")).into_iter()
            .filter(|e| e.kind == Kind::ApprovalAnswered)
            .collect();
        assert_eq!(resp.len(), 1, "a trama tem de registrar que a pendencia MORREU");
        assert!(resp[0].detail.as_deref().unwrap().contains("adaptador morreu"));
        std::fs::remove_dir_all(&dir).ok();
    }
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test permissao_de_edicao morte_do_filho a_opcao_sai 2>&1 | tail -15
```

Esperado: FAIL — `cannot find function from_acp_permission`, `acp_option_id`, `nua_para_teste`.

- [ ] **Step 3: Implementar em `event.rs`**

```rust
/// O pedido de permissão do ACP → o evento que a tela de aprovação consome.
///
/// O diff vem PRONTO em `content[{type:"diff", oldText, newText}]`. No `codex.rs` isso foi
/// garimpado de campos não documentados; aqui é campo tipado e não se calcula nada.
pub fn from_acp_permission(lane: &str, id: &str, params: &serde_json::Value) -> AgentEvent {
    let kind = params
        .pointer("/toolCall/kind")
        .and_then(|x| x.as_str())
        .unwrap_or("");
    let titulo = params
        .pointer("/toolCall/title")
        .and_then(|x| x.as_str())
        .unwrap_or("permissao");

    let mut e = AgentEvent::new(lane, Kind::AwaitingApproval, Confidence::Exact).with_text(titulo);
    e.approval_id = Some(id.to_string());
    e.approval_method = Some("session/request_permission".into());
    e.approval_kind = Some(ApprovalKind::from_acp_kind(kind));

    if let Some(cs) = params.pointer("/toolCall/content").and_then(|x| x.as_array()) {
        for c in cs {
            if c.get("type").and_then(|x| x.as_str()) == Some("diff") {
                let p = c.get("path").and_then(|x| x.as_str()).unwrap_or("");
                let velho = c.get("oldText").and_then(|x| x.as_str()).unwrap_or("");
                let novo = c.get("newText").and_then(|x| x.as_str()).unwrap_or("");
                e.diff = Some(format!("--- {p}\n+++ {p}\n-{velho}\n+{novo}"));
                break;
            }
        }
    }
    e
}

/// Escolhe a opção pelo `kind` DECLARADO. `options[0]` foi medido como `reject_once`: escolher
/// por posição nega tudo, e a tela reporta sucesso.
pub fn acp_option_id(params: &serde_json::Value, allow: bool) -> Option<String> {
    let ops = params.get("options")?.as_array()?;
    let procura = |k: &str| {
        ops.iter()
            .find(|o| o.get("kind").and_then(|x| x.as_str()) == Some(k))
            .and_then(|o| o.get("optionId"))
            .and_then(|x| x.as_str())
            .map(str::to_string)
    };
    if allow {
        procura("allow_always").or_else(|| procura("allow_once"))
    } else {
        procura("reject_once").or_else(|| procura("reject_always"))
    }
}
```

- [ ] **Step 4: Implementar em `acp.rs`**

Acrescente ao `impl AcpLane`, e substitua o `cancelar_pendentes` vazio:

```rust
    /// Construtor só de teste: sem filho, sem stdio. O vazamento se prova no mapa de
    /// pendências, e subir um Node de verdade tornaria o teste lento e flaky sem provar mais.
    #[cfg(test)]
    pub(crate) fn nua_para_teste(
        lane: &str,
        trama: Arc<Trama>,
        modo: &str,
        sessao: Option<&str>,
    ) -> Arc<Self> {
        Arc::new(Self {
            lane: lane.to_string(),
            stdin: Arc::new(Mutex::new(None)),
            pending: Arc::new(Mutex::new(HashMap::new())),
            sessao: Arc::new(Mutex::new(sessao.map(str::to_string))),
            proximo_id: Arc::new(Mutex::new(10)),
            trama,
            modo: modo.to_string(),
            cwd: "/tmp".into(),
        })
    }

    /// Toda aprovação pendurada é resolvida como CANCELADA antes do respawn.
    ///
    /// Sem isto, a morte do filho deixa botões na tela que não respondem a ninguém — o único
    /// ponto deste desenho em que a falha produz UI mentirosa em vez de UI ausente.
    async fn cancelar_pendentes(&self, motivo: &str) {
        let mut g = self.pending.lock().await;
        for (id, tx) in g.drain() {
            let _ = tx.send(String::new());
            self.trama.append(
                AgentEvent::new(&self.lane, Kind::ApprovalAnswered, Confidence::Exact)
                    .detail(format!("aprovacao {id} cancelada: {motivo}")),
            );
        }
    }

    pub async fn answer(&self, approval_id: &str, allow: bool) -> Result<(), String> {
        let tx = self
            .pending
            .lock()
            .await
            .remove(approval_id)
            .ok_or_else(|| format!("aprovacao {approval_id} nao esta pendente"))?;
        let _ = tx.send(if allow { "allow".into() } else { "reject".into() });
        self.trama.append(
            AgentEvent::new(&self.lane, Kind::ApprovalAnswered, Confidence::Exact)
                .detail(format!("aprovacao {approval_id}: {}", if allow { "permitida" } else { "negada" })),
        );
        Ok(())
    }
```

E, em `on_message`, antes da chamada a `traduzir` — o pedido do agente é uma mensagem **com `id` e com `method`**:

```rust
        let metodo = m.get("method").and_then(|x| x.as_str()).unwrap_or("");
        if m.get("id").is_some() && metodo == "session/request_permission" {
            let rpc_id = m["id"].clone();
            let id = rpc_id.to_string();
            let params = m["params"].clone();

            let (tx, rx) = oneshot::channel::<String>();
            self.pending.lock().await.insert(id.clone(), tx);

            let mut e = crate::event::from_acp_permission(&self.lane, &id, &params);
            e.session = self.sessao.lock().await.clone();
            self.trama.append(e);

            // A resposta espera o operador em outra task: bloquear aqui pararia a leitura do
            // stdout, e com ela toda a observação da lane. O turno fica parado — que é o
            // comportamento correto, e a tela mostra "esperando você", não "travado".
            let stdin = self.stdin.clone();
            tokio::spawn(async move {
                let escolha = rx.await.unwrap_or_default();
                if escolha.is_empty() {
                    return; // cancelado pela morte do filho: não há a quem responder
                }
                let opt = crate::event::acp_option_id(&params, escolha == "allow");
                let resp = serde_json::json!({
                    "jsonrpc":"2.0","id":rpc_id,
                    "result":{"outcome":{"outcome":"selected","optionId":opt}}
                });
                let mut g = stdin.lock().await;
                if let Some(si) = g.as_mut() {
                    let _ = si.write_all(format!("{resp}\n").as_bytes()).await;
                    let _ = si.flush().await;
                }
            });
            return;
        }
```

- [ ] **Step 5: Rodar e ver passar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test permissao_de_edicao morte_do_filho a_opcao_sai 2>&1 | grep -E "^test result|panicked"
cargo test 2>&1 | grep -E "^test result|FAILED"
```

- [ ] **Step 6: Mutação**

```bash
cd /home/devsounio/beagle/crates/loomd
cp src/event.rs /tmp/ev.bak && cp src/acp.rs /tmp/acp.bak
# Mutação A: escolher por posição
python3 - <<'PY'
p="src/event.rs"; s=open(p).read()
a='''    if allow {
        procura("allow_always").or_else(|| procura("allow_once"))'''
assert a in s
s=s.replace(a,'''    if allow {
        ops.first().and_then(|o| o.get("optionId")).and_then(|x| x.as_str()).map(str::to_string)
        .or_else(|| procura("allow_once"))''',1)
open(p,"w").write(s)
PY
cargo test a_opcao_sai 2>&1 | grep -E "^test result"    # PRECISA falhar: reject em vez de allow
cp /tmp/ev.bak src/event.rs
# Mutação B: o vazamento volta
python3 - <<'PY'
p="src/acp.rs"; s=open(p).read()
a="        for (id, tx) in g.drain() {"
assert a in s
s=s.replace(a,"        for (id, tx) in std::iter::empty::<(String, oneshot::Sender<String>)>() {\n            let _ = (&id, &tx);\n        }\n        #[allow(unreachable_code)]\n        for (id, tx) in g.drain() {\n            if true { continue; }",1)
open(p,"w").write(s)
PY
cargo test morte_do_filho 2>&1 | grep -E "^test result|error" | head -3   # PRECISA falhar
cp /tmp/acp.bak src/acp.rs
cargo test 2>&1 | grep -E "^test result"
```

- [ ] **Step 7: Commit**

```bash
cd /home/devsounio/beagle
git add crates/loomd/src/event.rs crates/loomd/src/acp.rs
git commit -m "feat(loomd): aprovacao ACP — diff pronto, opcao por kind, e o vazamento fechado

O diff vem tipado no pedido (`content[{type:'diff',oldText,newText}]`) — no codex
isso foi garimpado. A opcao sai do `kind` declarado: `options[0]` foi MEDIDO como
`reject_once`, entao escolher por posicao nega tudo reportando sucesso.

A morte do filho resolve toda pendencia como cancelada. Sem isso ficam botoes na
tela que nao respondem a ninguem — o unico ponto deste desenho onde a falha
produz UI MENTIROSA em vez de UI ausente.

Responder acontece em outra task: bloquear a leitura do stdout pararia a
observacao inteira da lane.

Mutacoes: opcao por posicao -> vermelho; drain() neutralizado -> vermelho."
```

---

## Task 6: `prompt`, `interrupt`, `steer` e as rotas

**Files:**
- Modify: `src/acp.rs` (`prompt`, `interrupt`, `enfileirar`)
- Modify: `src/main.rs:30-34` (`App`), `:70-81` (rotas), `:131-232` (handlers)

**Interfaces:**
- Consumes: `AcpLane::{spawn, answer}` (Tasks 3 e 5).
- Produces:
  ```rust
  // acp.rs
  impl AcpLane {
      pub async fn prompt(&self, text: &str) -> Result<(), String>;
      pub async fn interrupt(&self) -> Result<(), String>;
      pub async fn enfileirar(&self, text: &str) -> Result<(), String>;
  }
  // main.rs
  enum LaneHandle { Codex(Arc<codex::CodexLane>), Acp(Arc<acp::AcpLane>) }
  ```

- [ ] **Step 1: Escrever o teste que falha**

Em `src/main.rs`, no `mod tests` existente (acrescente `use std::sync::Arc;` e `use crate::trama::Trama;` ao `mod tests` se ainda não estiverem lá):

```rust
    /// 🚨 `/steer` NÃO tem a mesma semântica nas duas classes de lane. No codex,
    /// `turn/steer` REDIRECIONA o turno em curso. O ACP não tem isso: ele anuncia
    /// `promptQueueing: true`, ou seja, outro prompt ENFILEIRA para depois.
    ///
    /// Semântica diferente sob o mesmo nome é armadilha. A resposta da rota tem de dizer qual
    /// das duas aconteceu, senão a tela mente para o operador.
    #[test]
    fn steer_declara_qual_semantica_aplicou() {
        assert_eq!(super::rotulo_de_steer(true), "redirecionado");
        assert_eq!(super::rotulo_de_steer(false), "enfileirado");
    }

    #[tokio::test]
    async fn steer_na_lane_acp_reporta_enfileirado() {
        let dir = std::env::temp_dir().join(format!("loomd-steer-{}", std::process::id()));
        std::fs::create_dir_all(&dir).unwrap();
        let t = Arc::new(Trama::open(dir.join("trama.jsonl")));
        // COM sessão: `enfileirar` chega a `Ok(())`, então o despacho tem de devolver
        // `Ok(false)` — asserção EXATA. A versão frouxa deste teste (`Err(_) | Ok(false)`)
        // passava com qualquer coisa menos `Ok(true)`, o que é quase não assertar nada.
        let h = super::LaneHandle::Acp(crate::acp::AcpLane::nua_para_teste(
            "claude-4", t, "default", Some("sess-1"),
        ));
        assert_eq!(h.steer("oi").await, Ok(false), "lane ACP ENFILEIRA, nunca redireciona");
        std::fs::remove_dir_all(&dir).ok();
    }
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test steer_declara 2>&1 | tail -8
```

Esperado: FAIL — `cannot find function rotulo_de_steer`.

- [ ] **Step 3: Implementar em `acp.rs`**

```rust
    async fn novo_id(&self) -> u64 {
        let mut g = self.proximo_id.lock().await;
        *g += 1;
        *g
    }

    pub async fn prompt(&self, text: &str) -> Result<(), String> {
        let sid = self.sessao.lock().await.clone().ok_or("sessao ACP ainda nao aberta")?;
        let id = self.novo_id().await;
        self.enviar(serde_json::json!({
            "jsonrpc":"2.0","id":id,"method":"session/prompt",
            "params":{"sessionId": sid, "prompt":[{"type":"text","text": text}]}
        }))
        .await;
        let mut e = AgentEvent::new(&self.lane, Kind::UserPrompt, Confidence::Exact).with_text(text);
        e.session = Some(sid);
        self.trama.append(e);
        Ok(())
    }

    pub async fn interrupt(&self) -> Result<(), String> {
        let sid = self.sessao.lock().await.clone().ok_or("sessao ACP ainda nao aberta")?;
        self.enviar(serde_json::json!({
            "jsonrpc":"2.0","method":"session/cancel","params":{"sessionId": sid}
        }))
        .await;
        Ok(())
    }

    /// O que o ACP tem no lugar do `turn/steer` do codex: `promptQueueing: true`. O prompt vai
    /// para a FILA, não redireciona o turno em curso. Mesmo canal do `prompt` de propósito —
    /// dois caminhos para a mesma ação divergem.
    pub async fn enfileirar(&self, text: &str) -> Result<(), String> {
        self.prompt(text).await
    }
```

- [ ] **Step 4: Implementar em `main.rs`**

Acrescente o rótulo e o handle:

```rust
/// Qual semântica o `/steer` aplicou. O operador precisa saber: redirecionar o turno em curso e
/// enfileirar para depois são coisas diferentes, e só o codex faz a primeira.
pub fn rotulo_de_steer(redireciona: bool) -> &'static str {
    if redireciona { "redirecionado" } else { "enfileirado" }
}

/// As duas classes de lane supervisionada. Enum e não trait: são exatamente duas, os métodos
/// são quatro, e um trait com `async fn` aqui só acrescentaria `Box<dyn>` e ruído.
#[derive(Clone)]
enum LaneHandle {
    Codex(Arc<codex::CodexLane>),
    Acp(Arc<acp::AcpLane>),
}

impl LaneHandle {
    async fn prompt(&self, t: &str) -> Result<(), String> {
        match self {
            LaneHandle::Codex(l) => l.prompt(t).await,
            LaneHandle::Acp(l) => l.prompt(t).await,
        }
    }
    async fn interrupt(&self) -> Result<(), String> {
        match self {
            LaneHandle::Codex(l) => l.interrupt().await,
            LaneHandle::Acp(l) => l.interrupt().await,
        }
    }
    /// Devolve `true` quando REDIRECIONOU o turno; `false` quando só enfileirou.
    async fn steer(&self, t: &str) -> Result<bool, String> {
        match self {
            LaneHandle::Codex(l) => l.steer(t).await.map(|_| true),
            LaneHandle::Acp(l) => l.enfileirar(t).await.map(|_| false),
        }
    }
    async fn answer(&self, id: &str, allow: bool) -> Result<(), String> {
        match self {
            LaneHandle::Codex(l) => l.answer(id, allow).await,
            LaneHandle::Acp(l) => l.answer(id, allow).await,
        }
    }
}
```

Troque o campo de `App`:

```rust
#[derive(Clone)]
struct App {
    trama: Arc<Trama>,
    lanes: Arc<HashMap<String, LaneHandle>>,
}
```

Nos quatro handlers (`approve`, `prompt`, `interrupt`, `steer`), substitua `a.codex.get(&lane)` por `a.lanes.get(&lane)`. No handler `steer`, use o retorno:

```rust
    match h.steer(&texto).await {
        Ok(redirecionou) => Json(serde_json::json!({
            "ok": true, "efeito": rotulo_de_steer(redirecionou)
        })).into_response(),
        Err(e) => err(axum::http::StatusCode::BAD_GATEWAY, e).into_response(),
    }
```

E, em `main()`, depois do laço das lanes codex:

```rust
    // Lanes ACP: mesma forma `lane[:cwd]` do codex, variável própria.
    //   LOOMD_ACP_LANES="claude-4:/workspace/.wt/claude-4"
    let acp_bin = std::env::var("LOOMD_ACP_BIN").unwrap_or_else(|_| "claude-agent-acp".into());
    // O modo vem da Task 1 do plano — MEDIDO, não escolhido pela descrição.
    let acp_modo = std::env::var("LOOMD_ACP_MODO").unwrap_or_else(|_| "default".into());
    let acp_lanes = parse_lanes(&std::env::var("LOOMD_ACP_LANES").unwrap_or_default(), &cwd);
    for (l, c) in &acp_lanes {
        eprintln!("[loomd] lane ACP {l} em {c} (modo {acp_modo})");
        trama.declarar(l);
        map.insert(
            l.clone(),
            LaneHandle::Acp(acp::AcpLane::spawn(l, &acp_bin, c, &acp_modo, trama.clone())),
        );
    }
```

O laço do codex passa a inserir `LaneHandle::Codex(...)`.

- [ ] **Step 5: Rodar e ver passar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo fmt && cargo clippy --all-targets 2>&1 | grep -E "^error" | head
cargo test 2>&1 | grep -E "^test result|FAILED"
```

- [ ] **Step 6: Mutação**

```bash
cd /home/devsounio/beagle/crates/loomd
cp src/main.rs /tmp/main.bak
sed -i 's/LaneHandle::Acp(l) => l.enfileirar(t).await.map(|_| false)/LaneHandle::Acp(l) => l.enfileirar(t).await.map(|_| true)/' src/main.rs
cargo test steer_declara 2>&1 | grep -E "^test result"   # o teste do rotulo continua verde…
grep -n 'map(|_| true)' src/main.rs                       # …mas a mentira esta AQUI
cp /tmp/main.bak src/main.rs
```

A mutação **precisa** ficar vermelha por causa do teste de despacho abaixo, que já nasce no Step 1 desta tarefa — `rotulo_de_steer` é puro e não pegaria a mentira sozinho:

```rust
```

`nua_para_teste` já é `pub(crate)` na Task 5, então o `main.rs` a alcança em teste.

- [ ] **Step 7: Commit**

```bash
cd /home/devsounio/beagle
git add crates/loomd/src/acp.rs crates/loomd/src/main.rs
git commit -m "feat(loomd): rotas atendem as duas classes de lane, e /steer diz qual semantica usou

Nao criei /decide: /v2/lanes/:lane/approve JA EXISTE (main.rs:77) e passou a
despachar para as duas classes. Duas rotas para a mesma acao divergem — este
projeto ja pagou para aprender isso.

/steer e a armadilha: no codex `turn/steer` REDIRECIONA o turno em curso; o ACP
so tem promptQueueing, que ENFILEIRA. A rota devolve `efeito`, e a UI le dali.

Mutacao: enfileirar reportando `true` -> vermelho (depois de acrescentar o teste
de despacho, porque o teste do rotulo puro NAO pegava — anotado no plano)."
```

---

## Task 7: `TranscriptTail` — as 3 lanes atuais a `confidence: exact`

**Files:**
- Create: `src/transcript.rs`
- Modify: `src/event.rs` (`from_transcript_line`)
- Modify: `src/main.rs` (declarar módulo, subir os tails)

**Interfaces:**
- Consumes: `Trama::append` (existente).
- Produces:
  ```rust
  // event.rs
  pub fn from_transcript_line(lane: &str, l: &serde_json::Value) -> Option<AgentEvent>;
  // transcript.rs
  pub struct TranscriptTail;
  impl TranscriptTail {
      pub fn spawn(lane: &str, base: &str, trama: Arc<Trama>) -> Arc<Self>;
  }
  pub fn arquivo_mais_novo(dir: &std::path::Path) -> Option<std::path::PathBuf>;
  pub fn offset_seguro(offset: u64, tamanho: u64) -> u64;
  ```

- [ ] **Step 1: Reconfirmar o censo nos arquivos VIVOS**

Os tipos de linha foram censados no diretório antigo. O spec manda reconfirmar aqui, porque essa exata suposição já errou uma vez.

```bash
export KUBECONFIG=/home/devsounio/.kube/config
kubectl -n beagle exec sounio-workspace-control-0 -c workspace-ssh -- \
  su -s /bin/bash openvscode-server -c '
python3 - <<"PY"
import json, glob, collections, os
B="/workspace/.home/openvscode-server/.agents"
for a in ("claude-1","claude-2","claude-3"):
    fs=glob.glob(f"{B}/{a}/.claude/projects/*/*.jsonl")
    if not fs: print(a,"SEM transcript"); continue
    f=max(fs,key=os.path.getmtime)
    c=collections.Counter()
    for l in open(f,errors="replace"):
        l=l.strip()
        if l:
            try: c[json.loads(l).get("type","?")]+=1
            except Exception: c["ILEGIVEL"]+=1
    print(a, dict(c.most_common(8)))
PY'
```

**Confirme que `assistant`, `user`, `last-prompt` e `ai-title` aparecem.** Se algum tipo esperado não existir, ajuste a tabela do Step 3 ao que foi medido — a medição vence o plano.

- [ ] **Step 2: Escrever os testes que falham**

Crie `src/transcript.rs`:

```rust
//! Observa uma lane TUI lendo o transcript que o Claude Code JÁ escreve.
//!
//! 🚨 O CAMINHO TEM PEGADINHA, e ela quase derrubou o desenho: cada lane tem HOME próprio. Não é
//! `~/.claude/projects/`, é `.agents/<lane>/.claude/projects/<cwd-slug>/<sessao>.jsonl`. O
//! diretório compartilhado estava parado há quatro dias enquanto as lanes escreviam milhares de
//! linhas nos delas.
//!
//! Read-only por decisão: as 3 lanes estão trabalhando com `defaultMode: auto` e o TUI intacto.
//! Isto substitui os hooks — que continuam no código para o dia em que for preciso INTERCEPTAR;
//! para OBSERVAR, o arquivo já está lá, com fidelidade total em vez de só onde o hook dispara.

use crate::trama::Trama;
use std::sync::Arc;

pub struct TranscriptTail;

/// A sessão corrente é o `.jsonl` de mtime mais novo. Sessão nova = arquivo novo, então isto
/// também é a detecção de rotação.
pub fn arquivo_mais_novo(_dir: &std::path::Path) -> Option<std::path::PathBuf> {
    None
}

/// Truncamento: se o offset guardado passou do fim do arquivo, o arquivo encolheu e o offset é
/// lixo — recomeça do zero. Arquivo pede esta guarda; socket não.
pub fn offset_seguro(_offset: u64, _tamanho: u64) -> u64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn offset_maior_que_o_arquivo_recomeca_do_zero() {
        assert_eq!(offset_seguro(500, 900), 500, "offset valido e respeitado");
        assert_eq!(offset_seguro(900, 900), 900, "fim exato nao e truncamento");
        assert_eq!(offset_seguro(1_000, 900), 0, "arquivo encolheu: offset e lixo");
    }

    #[test]
    fn escolhe_o_jsonl_de_mtime_mais_novo() {
        let dir = std::env::temp_dir().join(format!("loomd-tt-{}", std::process::id()));
        std::fs::create_dir_all(&dir).unwrap();
        std::fs::write(dir.join("velha.jsonl"), "{}\n").unwrap();
        std::thread::sleep(std::time::Duration::from_millis(1_100));
        std::fs::write(dir.join("nova.jsonl"), "{}\n").unwrap();
        std::fs::write(dir.join("ruido.txt"), "nao e transcript").unwrap();

        let f = arquivo_mais_novo(&dir).expect("tem de achar um");
        assert_eq!(f.file_name().unwrap(), "nova.jsonl");
        std::fs::remove_dir_all(&dir).ok();
    }

    #[test]
    fn diretorio_sem_transcript_devolve_nada() {
        let dir = std::env::temp_dir().join(format!("loomd-tt-vazio-{}", std::process::id()));
        std::fs::create_dir_all(&dir).unwrap();
        assert!(arquivo_mais_novo(&dir).is_none());
        std::fs::remove_dir_all(&dir).ok();
    }
}
```

Em `src/event.rs`, no `mod tests`:

```rust
    #[test]
    fn ai_title_do_transcript_vira_o_titulo_da_lane() {
        // O Claude Code NOMEIA o que está fazendo (298 ocorrências no censo). É melhor que o
        // título OSC raspado do tmux, que é o que a Frota usava.
        let l = serde_json::json!({"type":"ai-title","title":"Consertar o gate do stdlib"});
        let e = from_transcript_line("claude-2", &l).expect("ai-title e informacao");
        assert_eq!(e.kind, Kind::SessionStarted);
        assert_eq!(e.confidence, Confidence::Exact, "transcript e fato, nao inferencia");
        assert_eq!(e.detail.as_deref(), Some("Consertar o gate do stdlib"));
    }

    #[test]
    fn last_prompt_do_transcript_vira_userprompt_exato() {
        let l = serde_json::json!({"type":"last-prompt","prompt":"roda o gate de confiabilidade"});
        let e = from_transcript_line("claude-2", &l).unwrap();
        assert_eq!(e.kind, Kind::UserPrompt);
        assert_eq!(e.confidence, Confidence::Exact);
        assert_eq!(e.text.as_deref(), Some("roda o gate de confiabilidade"));
    }

    #[test]
    fn linha_de_transcript_sem_valor_de_ui_nao_polui_a_trama() {
        for t in ["queue-operation", "file-history-snapshot", "pr-link", "attachment"] {
            let l = serde_json::json!({"type": t});
            assert!(from_transcript_line("claude-2", &l).is_none(), "{t} nao e evento de UI");
        }
    }
```

- [ ] **Step 3: Rodar e ver falhar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo test transcript ai_title last_prompt linha_de_transcript 2>&1 | tail -15
```

Esperado: FAIL — `cannot find function from_transcript_line`, e os três de `transcript.rs`.

- [ ] **Step 4: Implementar**

Em `src/event.rs`:

```rust
/// Traduz uma linha do transcript do Claude Code.
///
/// `Confidence::Exact` — e essa é a tese: até aqui as lanes Claude eram `Inferred` porque o
/// loomd raspava a tela do tmux. O transcript é o que o próprio agente gravou.
pub fn from_transcript_line(lane: &str, l: &serde_json::Value) -> Option<AgentEvent> {
    let tipo = l.get("type").and_then(|x| x.as_str()).unwrap_or("");
    match tipo {
        "ai-title" => {
            let t = l.get("title").and_then(|x| x.as_str())?;
            Some(AgentEvent::new(lane, Kind::SessionStarted, Confidence::Exact).detail(t))
        }
        "last-prompt" => {
            let p = l.get("prompt").and_then(|x| x.as_str())?;
            Some(AgentEvent::new(lane, Kind::UserPrompt, Confidence::Exact).with_text(p))
        }
        "assistant" => {
            // A fala vive em `message.content[]`; blocos de `tool_use` são ferramenta, não fala.
            let cs = l.pointer("/message/content")?.as_array()?;
            if let Some(tu) = cs.iter().find(|c| c.get("type").and_then(|x| x.as_str()) == Some("tool_use")) {
                let mut e = AgentEvent::new(lane, Kind::ToolCall, Confidence::Exact);
                e.tool = tu.get("name").and_then(|x| x.as_str()).map(str::to_string);
                return Some(e);
            }
            let txt: String = cs
                .iter()
                .filter_map(|c| c.get("text").and_then(|x| x.as_str()))
                .collect::<Vec<_>>()
                .join("");
            if txt.trim().is_empty() {
                return None;
            }
            Some(AgentEvent::new(lane, Kind::AgentMessage, Confidence::Exact).with_text(txt))
        }
        "user" => {
            let cs = l.pointer("/message/content")?.as_array()?;
            cs.iter()
                .find(|c| c.get("type").and_then(|x| x.as_str()) == Some("tool_result"))
                .map(|_| AgentEvent::new(lane, Kind::ToolResult, Confidence::Exact))
        }
        "mode" | "permission-mode" => {
            let m = l.get("mode").and_then(|x| x.as_str()).unwrap_or("?");
            Some(AgentEvent::new(lane, Kind::Unknown, Confidence::Exact).detail(format!("modo: {m}")))
        }
        _ => None,
    }
}
```

Em `src/transcript.rs`:

```rust
pub fn arquivo_mais_novo(dir: &std::path::Path) -> Option<std::path::PathBuf> {
    let mut melhor: Option<(std::time::SystemTime, std::path::PathBuf)> = None;
    for e in std::fs::read_dir(dir).ok()? {
        let p = e.ok()?.path();
        if p.extension().and_then(|x| x.to_str()) != Some("jsonl") {
            continue;
        }
        let mt = p.metadata().ok()?.modified().ok()?;
        if melhor.as_ref().map_or(true, |(m, _)| mt > *m) {
            melhor = Some((mt, p));
        }
    }
    melhor.map(|(_, p)| p)
}

pub fn offset_seguro(offset: u64, tamanho: u64) -> u64 {
    if offset > tamanho {
        0
    } else {
        offset
    }
}

impl TranscriptTail {
    /// `base` é o diretório de projetos da lane, por exemplo
    /// `/workspace/.home/openvscode-server/.agents/claude-2/.claude/projects/-workspace-sounio`.
    pub fn spawn(lane: &str, base: &str, trama: Arc<Trama>) -> Arc<Self> {
        let (l, b) = (lane.to_string(), base.to_string());
        tokio::spawn(async move {
            let dir = std::path::PathBuf::from(&b);
            let mut atual: Option<std::path::PathBuf> = None;
            let mut offset: u64 = 0;
            loop {
                if let Some(f) = arquivo_mais_novo(&dir) {
                    // Rotação: sessão nova é arquivo novo, e o offset do anterior não vale nele.
                    if atual.as_ref() != Some(&f) {
                        atual = Some(f.clone());
                        offset = 0;
                    }
                    let tam = std::fs::metadata(&f).map(|m| m.len()).unwrap_or(0);
                    offset = offset_seguro(offset, tam);
                    if tam > offset {
                        if let Ok(bytes) = ler_de(&f, offset) {
                            offset = tam;
                            for linha in String::from_utf8_lossy(&bytes).lines() {
                                let Ok(v) = serde_json::from_str::<serde_json::Value>(linha) else {
                                    continue;
                                };
                                if let Some(e) = crate::event::from_transcript_line(&l, &v) {
                                    trama.append(e);
                                }
                            }
                        }
                    }
                }
                tokio::time::sleep(std::time::Duration::from_millis(1_500)).await;
            }
        });
        Arc::new(Self)
    }
}

fn ler_de(p: &std::path::Path, offset: u64) -> std::io::Result<Vec<u8>> {
    use std::io::{Read, Seek, SeekFrom};
    let mut f = std::fs::File::open(p)?;
    f.seek(SeekFrom::Start(offset))?;
    let mut v = Vec::new();
    f.read_to_end(&mut v)?;
    Ok(v)
}
```

Em `src/main.rs`, declare `mod transcript;` e suba os tails depois das lanes ACP:

```rust
    // Observação read-only das lanes TUI. Forma `lane:dir-de-projetos`, vírgula entre elas.
    //   LOOMD_TAILS="claude-1:/workspace/.home/openvscode-server/.agents/claude-1/.claude/projects/-workspace-sounio,…"
    for (l, d) in parse_lanes(&std::env::var("LOOMD_TAILS").unwrap_or_default(), "") {
        if d.is_empty() {
            eprintln!("[loomd] tail {l} SEM diretorio — ignorado");
            continue;
        }
        eprintln!("[loomd] tail {l} em {d}");
        trama.declarar(&l);
        transcript::TranscriptTail::spawn(&l, &d, trama.clone());
    }
```

- [ ] **Step 5: Rodar e ver passar**

```bash
cd /home/devsounio/beagle/crates/loomd
cargo fmt && cargo clippy --all-targets 2>&1 | grep -E "^error" | head
cargo test 2>&1 | grep -E "^test result|FAILED"
```

- [ ] **Step 6: Mutação**

```bash
cd /home/devsounio/beagle/crates/loomd
cp src/transcript.rs /tmp/tt.bak && cp src/event.rs /tmp/ev.bak
# Mutação A: guarda de truncamento some
python3 - <<'PY'
p="src/transcript.rs"; s=open(p).read()
a="    if offset > tamanho {"
assert a in s
s=s.replace(a,"    if false {",1); open(p,"w").write(s)
PY
cargo test offset_maior 2>&1 | grep -E "^test result"      # PRECISA falhar
cp /tmp/tt.bak src/transcript.rs
# Mutação B: transcript volta a ser Inferred — a tese inteira
python3 - <<'PY'
p="src/event.rs"; s=open(p).read()
n=s.count('Kind::UserPrompt, Confidence::Exact).with_text(p)')
assert n==1
s=s.replace('Kind::UserPrompt, Confidence::Exact).with_text(p)',
            'Kind::UserPrompt, Confidence::Inferred).with_text(p)',1)
open(p,"w").write(s)
PY
cargo test last_prompt 2>&1 | grep -E "^test result"       # PRECISA falhar
cp /tmp/ev.bak src/event.rs
cargo test 2>&1 | grep -E "^test result"
```

- [ ] **Step 7: Commit**

```bash
cd /home/devsounio/beagle
git add crates/loomd/src/transcript.rs crates/loomd/src/event.rs crates/loomd/src/main.rs
git commit -m "feat(loomd): lanes TUI a confidence=exact lendo o transcript que elas ja escrevem

Substitui os hooks, e a troca foi decidida DEPOIS de medir: o arquivo esta la com
fidelidade total, enquanto o hook so ve onde dispara. E nao exige uma linha de
configuracao em lane que esta trabalhando.

A pegadinha: cada lane tem HOME PROPRIO em .agents/<lane>/.claude/projects/ — o
diretorio compartilhado estava parado ha 4 dias enquanto as lanes escreviam
milhares de linhas. O primeiro palpite do spec apontava para o arquivo morto.

`ai-title` de brinde: o Claude Code nomeia o que esta fazendo, melhor que o
titulo OSC raspado do tmux.

Mutacoes: guarda de truncamento removida -> vermelho; Exact -> Inferred ->
vermelho (e essa e a tese inteira da fatia)."
```

---

## Task 8: Subir e provar ao vivo

**Files:**
- Modify: `docs/superpowers/specs/2026-08-10-lane-acp-design.md` (seção de resultado)

**Interfaces:**
- Consumes: tudo.
- Produces: a lane `claude-4` supervisionada por protocolo e os 3 tails ativos.

- [ ] **Step 1: Node 22 e o adaptador no pod**

```bash
export KUBECONFIG=/home/devsounio/.kube/config
kubectl -n beagle exec sounio-workspace-control-0 -c workspace-ssh -- \
  su -s /bin/bash openvscode-server -c '
export HOME=/workspace/.home/openvscode-server
export PATH="$HOME/.local/bin:$PATH"
node -v 2>&1 | head -1
npm install -g --prefix "$HOME/.local" @agentclientprotocol/claude-agent-acp@0.66.0 2>&1 | tail -3
"$HOME/.local/bin/claude-agent-acp" --version 2>&1 | head -2'
```

Se `node` não estiver no PATH, instale Node 22 no prefixo do usuário — **nunca como root**, e **pinado**, que é a prática já vigente neste workspace.

- [ ] **Step 2: A worktree da lane**

```bash
export KUBECONFIG=/home/devsounio/.kube/config
kubectl -n beagle exec sounio-workspace-control-0 -c workspace-ssh -- \
  su -s /bin/bash openvscode-server -c '
cd /workspace/sounio
git worktree list | grep -q claude-4 || \
  git worktree add /workspace/.wt/claude-4 -b lane/claude-4 integration/sounio-dev-ready-base
ls -d /workspace/.wt/claude-4'
```

Worktree própria pelo mesmo motivo do `codex-4`: duas lanes na mesma árvore sobrescrevem edição uma da outra sem conflito de git para denunciar.

- [ ] **Step 3: Subir o loomd com as três coisas ligadas**

Use o modo decidido na Task 1 em `LOOMD_ACP_MODO`.

```bash
export KUBECONFIG=/home/devsounio/.kube/config
kubectl -n beagle exec sounio-workspace-control-0 -c workspace-ssh -- \
  su -s /bin/bash openvscode-server -c '
export HOME=/workspace/.home/openvscode-server
export PATH="$HOME/.local/bin:$PATH"
B=$HOME/.agents
export LOOMD_ACP_LANES="claude-4:/workspace/.wt/claude-4"
export LOOMD_ACP_BIN="$HOME/.local/bin/claude-agent-acp"
export LOOMD_ACP_MODO="default"
export LOOMD_TAILS="claude-1:$B/claude-1/.claude/projects/-workspace-sounio,claude-2:$B/claude-2/.claude/projects/-workspace-sounio,claude-3:$B/claude-3/.claude/projects/-workspace-sounio"
cd /workspace/loomd && cargo build --release 2>&1 | tail -3
(setsid ./target/release/loomd >> /workspace/.loomd/loomd.log 2>&1 &)
sleep 6; curl -s localhost:4400/livez; echo; tail -8 /workspace/.loomd/loomd.log'
```

- [ ] **Step 4: Provar a lane ACP com um turno de verdade**

```bash
export KUBECONFIG=/home/devsounio/.kube/config
kubectl -n beagle exec sounio-workspace-control-0 -c workspace-ssh -- \
  su -s /bin/bash openvscode-server -c '
curl -sS -X POST localhost:4400/v2/lanes/claude-4/prompt \
  -H "content-type: application/json" --data-binary @- <<JSON
{"text":"Liste os arquivos de stdlib/epistemic/ e diga em uma frase o que cada um faz."}
JSON
echo; sleep 45
curl -s "localhost:4400/v2/trama?lane=claude-4" | python3 -c "
import json,sys,collections
d=json.load(sys.stdin)
ev=d.get(\"events\", d if isinstance(d,list) else [])
c=collections.Counter(e[\"kind\"] for e in ev)
print(\"eventos:\", dict(c))
print(\"confidence:\", set(e[\"confidence\"] for e in ev))
"'
```

**Critério:** aparecem `session_started`, `user_prompt`, `agent_message`, `tool_call`/`tool_result`, e **`usage`**. Todos `exact`. Se só houver `session_started`, o handshake subiu e o prompt não — olhe `LOOMD_DEBUG=1` no log.

- [ ] **Step 5: Provar que uma aprovação aparece e é respondível**

Só se a Task 1 fixou `default` (em `auto` o classificador pode não pedir):

```bash
export KUBECONFIG=/home/devsounio/.kube/config
kubectl -n beagle exec sounio-workspace-control-0 -c workspace-ssh -- \
  su -s /bin/bash openvscode-server -c '
curl -sS -X POST localhost:4400/v2/lanes/claude-4/prompt \
  -H "content-type: application/json" --data-binary @- <<JSON
{"text":"Crie /workspace/.wt/claude-4/NOTA_LANE.md com uma linha dizendo que esta lane e dirigida por protocolo ACP."}
JSON
echo; sleep 25
ID=$(curl -s "localhost:4400/v2/trama?lane=claude-4" | python3 -c "
import json,sys
d=json.load(sys.stdin); ev=d.get(\"events\", d if isinstance(d,list) else [])
p=[e for e in ev if e[\"kind\"]==\"awaiting_approval\"]
print(p[-1][\"approval_id\"] if p else \"\")")
echo "aprovacao pendente: [$ID]"
[ -n "$ID" ] && curl -sS -X POST localhost:4400/v2/lanes/claude-4/approve \
  -H "content-type: application/json" -d "{\"approval_id\":\"$ID\",\"allow\":true}"
echo; sleep 20; ls -la /workspace/.wt/claude-4/NOTA_LANE.md'
```

**Critério:** o `approval_id` existe, o `approve` responde `ok`, e o arquivo aparece. Se o evento traz `diff`, o §2 do spec está provado ponta a ponta.

- [ ] **Step 6: Provar os tails**

```bash
export KUBECONFIG=/home/devsounio/.kube/config
kubectl -n beagle exec sounio-workspace-control-0 -c workspace-ssh -- \
  su -s /bin/bash openvscode-server -c '
for a in claude-1 claude-2 claude-3; do
  curl -s "localhost:4400/v2/trama?lane=$a" | python3 -c "
import json,sys,collections
d=json.load(sys.stdin); ev=d.get(\"events\", d if isinstance(d,list) else [])
c=collections.Counter(e[\"confidence\"] for e in ev)
print(\"$a\", len(ev), \"eventos\", dict(c))"
done'
```

**Critério:** as três com eventos e `confidence: exact`. Zero eventos numa lane que tem transcript = o diretório está errado; confira contra o Step 1 da Task 7.

- [ ] **Step 7: Registrar o resultado no spec e commitar**

Acrescente ao fim do spec:

```markdown
## Resultado (Task 8 do plano)

**Provado ao vivo em <data>:** `claude-4` dirigida por ACP — turno completo com
`session_started`, `user_prompt`, `agent_message`, `tool_call`/`tool_result` e `usage`, todos
`exact`. Aprovação apareceu com `approval_id` e `diff`, foi respondida por
`/v2/lanes/claude-4/approve`, e o arquivo foi escrito. Os 3 tails levaram `claude-1..3` a
`confidence: exact` sem uma linha de configuração nelas.
```

```bash
cd /home/devsounio/beagle
git add docs/superpowers/specs/2026-08-10-lane-acp-design.md
git commit -m "docs(loomd): lane ACP provada ao vivo

Turno completo, aprovacao respondida com diff, e as 3 lanes TUI em exact.
Registrado no spec porque 'passou nos testes' e 'funciona na lane' sao afirmacoes
diferentes — e este projeto ja confundiu as duas."
```

---

## As 5 mutações do spec — onde cada uma é pega

Feito o mapa de propósito, porque uma mutação "coberta em algum lugar" é como um teste teatral:
parece rede e não é.

| mutação do spec §5 | pega em |
|---|---|
| 1. Remover o `session/set_mode` | **Task 3, Step 4b** — `o_handshake_manda_set_mode_e_ele_e_o_ultimo`. Assertável porque o handshake é função pura; e novamente ao vivo na Task 8, Step 5. |
| 2. `allow_always` → `options[0]` | Task 5, Step 6, mutação A |
| 3. Persistir `tool_call_update` sem `status` | Task 4, Step 5, mutação A (fechamentos vão de 7 para 28) |
| 4. Matar o filho com aprovação pendente | Task 5, Step 6, mutação B |
| 5. Tail apontando para `~/.claude/projects/` | **só ao vivo**, Task 8 Step 6 — o diretório vem de `LOOMD_TAILS`, e um teste unitário que compara strings de caminho não prova nada sobre qual caminho tem dado. O que prova é a lane ter eventos. Declarado, não escondido. |

## Notas de risco carregadas do spec

- **Node ≥22.** Medido funcionando em v20.19.2. Se quebrar, pine Node 22 no prefixo do usuário.
- **`bypassPermissions` pode voltar** se uma versão futura ignorar `set_mode`. A rede é a mutação A da Task 4 e o Step 5 da Task 8.
- **`/steer` divergente** entre as classes: mitigado por rótulo na resposta da rota, e o teste de despacho da Task 6 é o que impede a mentira.
- **A extração da Task 2 toca o `codex.rs`.** Se um teste dele precisar de edição, a tarefa saiu de escopo.
