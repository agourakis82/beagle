# Workstream Recipes

This directory contains canonical execution recipes for Darwin/HPC workstreams.

Rules:

1. each recipe belongs to exactly one workstream
2. each recipe is versioned in the repo next to the workstream registry
3. each recipe must declare ordered steps, outputs and recovery points
4. adding a recipe does not make it canonical by itself; promotion still
   requires an explicit phase
