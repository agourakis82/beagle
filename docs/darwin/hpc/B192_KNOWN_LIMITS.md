# B19.2 — Known Limits

Status: GO

## Current Limits

- this phase resolves program/campaign context by bounded aggregation over
  existing workstream packets; it does not introduce a full runtime graph
- active campaign selection is priority-based (`canonical-live` before
  `canonical`) and remains intentionally simple
- evidence targets are linked canonically, not yet transformed into evidence
  packs
- multi-campaign execution/orchestration remains out of scope
