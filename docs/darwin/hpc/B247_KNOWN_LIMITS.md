# B24.7 — Known Limits

- calibration is replay-plus-shadow only; it does not yet consume a large historical corpus of prior runs
- staged thresholds are recommendations, not live policy mutations
- task-family coverage is bounded to the first canonical families: `implementation`, `analysis`, and `manuscript`
- operator decisions still remain the final authority for any future live threshold promotion
- calibration quality depends on the existing `B24.3`, `B24.5`, and `B24.6` artifacts being present on the same Beagle-owned identity
