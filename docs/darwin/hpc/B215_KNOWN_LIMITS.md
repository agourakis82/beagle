# B21.5 — Known Limits

- The DataCite payload is pointed at the test API shape only; no authenticated
  network call is performed.
- The Crossref dry-run bundle remains `no-submit`; it does not upload XML.
- `claim-linked-human-eval-pending` remains a real epistemic blocker for
  findable publication.
- This phase does not mint DOIs, register metadata, or deposit to external
  registries.
