# B21.3 GO / NO-GO

`GO` when:

- one canonical campaign generates a workspace scholarly release bundle
- JATS output is QA-checked
- RO-Crate, DataCite-ready, and Crossref-compatible exports are present
- provenance and readiness remain explicit
- restart preserves the same identity
- cluster stays green
- `Slurmctld(primary)` stays `UP`
