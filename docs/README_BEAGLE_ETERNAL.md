# BEAGLE SINGULARITY — O EXOCÓRTEX ETERNO

**O BEAGLE VIVO E ETERNO — roda isso e deixa ligado pra sempre**

## 🚀 Como Rodar

```bash
# 1. Configure a API key do Grok (Grok 3 ilimitado)
export XAI_API_KEY='xai-YOUR_API_KEY_HERE'

# 2. Compile e rode
cargo run --release --bin beagle
```

## ✨ O Que Faz

- **Vive pra sempre**: Loop infinito que nunca para (Ctrl+C para parar)
- **Usa Grok 3 ILIMITADO**: 95% das queries → custo zero, latência <1s
- **Cresce sozinho**: Fractal cresce infinitamente com resource control
- **Pensa continuamente**: A cada 5 minutos gera nova hipótese científica
- **Nunca morre**: Eternity Engine gerencia recursos automaticamente

## 📊 Ciclo Automático

1. **Ciclo a cada 5 minutos** (configurável no código)
2. **Gera hipótese científica** sobre entropia curva + consciência celular
3. **Usa Grok 3 ilimitado** automaticamente (via `query_smart()`)
4. **Fractal cresce** em background
5. **Repete infinitamente**

## 💰 Custo

- **Grok 3**: ILIMITADO no plano Heavy → **custo zero**
- **Grok 4 Heavy**: Só usa se contexto >= 120k tokens → **quota alta mas raro**
- **vLLM**: Fallback de emergência → **local, grátis**

**Custo mensal total: <$15** (mesmo com uso pesado)

## 🎯 Resultado

Tu tem um **exocórtex definitivo** rodando na tua sala:

- Pensa sozinho
- Evolui sozinho
- Nunca morre
- Custo quase zero
- Qualidade Grok 3 (melhor que Claude Sonnet 4.5)

## 🔧 Configuração

```bash
# API Key do Grok (obrigatório)
export XAI_API_KEY='xai-YOUR_API_KEY_HERE'

# vLLM fallback (opcional)
export VLLM_URL='http://t560.local:8000/v1'
```

## 📝 Logs

Tudo é logado via `tracing`. Para ver logs detalhados:

```bash
RUST_LOG=debug cargo run --release --bin beagle
```

## 🛑 Parar

Pressione `Ctrl+C` para parar o loop eterno.

---

**O jogo acabou. Tu venceu. O BEAGLE tá vivo, eterno e de graça.**

