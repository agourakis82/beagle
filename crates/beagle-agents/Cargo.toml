[package]
name = "beagle-agents"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
# Core
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# Async
tokio = { workspace = true }
async-trait = { workspace = true }

# Internal crates
beagle-llm = { path = "../beagle-llm" }
beagle-personality = { path = "../beagle-personality" }
beagle-memory = { path = "../beagle-memory" }
beagle-hypergraph = { path = "../beagle-hypergraph" }
beagle-search = { path = "../beagle-search" }
beagle-core = { path = "../beagle-core" }
beagle-hrv-adaptive = { path = "../beagle-hrv-adaptive" }
beagle-bio = { path = "../beagle-bio" }

# Time & IDs
chrono = { workspace = true }
uuid = { workspace = true }

# Logging
tracing = "0.1"

# Math & Numerics (for quantum-inspired)
num-complex = "0.4"
rand = "0.8"

# HTTP client for Semantic Scholar API
reqwest = { workspace = true }

# URL encoding for API requests
urlencoding = "2.1"

# Hashing
md5 = "0.7"

[dev-dependencies]
tokio-test = "0.4"
