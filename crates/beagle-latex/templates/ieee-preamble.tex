% IEEE Conference/Journal Style - Preamble
% Two-column format

% Use IEEE document class features
\usepackage[conference]{IEEEtran}

% Two column layout
\usepackage{multicol}

% Compact margins
\usepackage[margin=1.8cm]{geometry}

% Times font (IEEE standard)
\usepackage{fontspec}
\setmainfont{Times New Roman}

% Math packages
\usepackage{amsmath}
\usepackage{amssymb}

% Graphics
\usepackage{graphicx}
\usepackage{subfig}

% Tables
\usepackage{booktabs}
\usepackage{multirow}

% Algorithms (IEEE style)
\usepackage{algorithm}
\usepackage{algorithmic}

% References (IEEE numbered style)
\usepackage{cite}

% URLs
\usepackage{url}

% Hyperlinks
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=black,
}

% IEEE specific spacing
\setlength{\columnsep}{0.25in}
\setlength{\parindent}{1em}
