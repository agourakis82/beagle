version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: beagle-postgres-dev
    environment:
      POSTGRES_USER: beagle
      POSTGRES_PASSWORD: beagle_dev_password
      POSTGRES_DB: beagle_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U beagle"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: beagle-redis-dev
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Neo4j (opcional - para hypergraph avançado)
  # neo4j:
  #   image: neo4j:5-community
  #   container_name: beagle-neo4j-dev
  #   environment:
  #     NEO4J_AUTH: neo4j/beagle_dev_password
  #   ports:
  #     - "7474:7474"  # HTTP
  #     - "7687:7687"  # Bolt
  #   volumes:
  #     - neo4j_data:/data

volumes:
  postgres_data:
  redis_data:
  # neo4j_data:
