apiVersion: v1
kind: Service
metadata:
  name: ssm-probe-serving
  namespace: beagle
  labels:
    app.kubernetes.io/name: ssm-probe-serving
    app.kubernetes.io/part-of: beagle
    sounio.dev/lane: ssm-probe
spec:
  selector:
    app.kubernetes.io/name: ssm-probe-serving
  ports:
    - name: http
      port: 8002
      targetPort: 8000
