apiVersion: v1
kind: Service
metadata:
  name: sglang-serving
  namespace: beagle
  labels:
    app.kubernetes.io/name: sglang-serving
    app.kubernetes.io/part-of: beagle
    nvidia.com/dynamo-discovery-backend: kubernetes
    nvidia.com/dynamo-discovery-enabled: "true"
    sounio.dev/state: scaffold
spec:
  selector:
    app.kubernetes.io/name: sglang-serving
  ports:
    - name: http
      port: 30000
      targetPort: http
