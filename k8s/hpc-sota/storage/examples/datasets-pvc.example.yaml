apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: training-datasets
  namespace: beagle
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-ssd-datasets
  resources:
    requests:
      storage: 2Ti
