nfd:
  enabled: true

sriovNetworkOperator:
  enabled: false

maintenanceOperator:
  enabled: false

operator:
  nodeSelector:
    kubernetes.io/hostname: t560-proxmox
  tolerations:
    - key: node-role.kubernetes.io/control-plane
      operator: Exists
      effect: NoSchedule
