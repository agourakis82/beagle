apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beagle-workspace-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  volumeMode: Filesystem
  storageClassName: ceph-rbd-ssd
