apiVersion: v1
kind: Service
metadata:
  name: beagle-sovereign-embeddings
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: beagle-sovereign-embeddings
  ports:
    - name: http
      port: 80
      targetPort: http
