apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: beagle-kube-apiserver
  namespace: tailscale
spec:
  type: kube-apiserver
  replicas: 2
  tags:
    - tag:k8s
  kubeAPIServer:
    mode: auth
    hostname: beagle-kube
