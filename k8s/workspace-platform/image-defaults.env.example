# Optional workspace-platform image defaults.
#
# Copy this file to:
#   /home/devsounio/beagle/k8s/workspace-platform/image-defaults.env
# when you want new scaffolds to prefer pullable registry refs instead of the
# fallback node-local localhost/... images.

DEFAULT_PROJECT_COCKPIT_IMAGE=localhost/project-cockpit:20260406-8
DEFAULT_IDE_IMAGE=localhost/sounio-workspace-ide:0406231500
DEFAULT_SSH_IMAGE=localhost/beagle-workspace-ssh:b216
