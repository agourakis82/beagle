<?xml version="1.0" encoding="UTF-8"?>
<!--
  MODELO. Copie para Secrets.plist e preencha com o valor real.

  Secrets.plist é IGNORADO pelo git de propósito: o token que ficava hardcoded em
  BeagleClient.swift esteve publicamente legível no GitHub de 01-jul a 06-ago-2026
  (branch integration/ios-physiome-merge). Qualquer pessoa podia conversar com o
  companion e baixar 19 KB da biografia dele via /companion/grounding.

  O valor real vive no cluster:
    kubectl -n beagle get secret cockpit-mobile-auth \
      -o jsonpath='{.data.PROJECT_COCKPIT_AUTH_TOKEN}' | base64 -d
-->
<plist version="1.0">
<dict>
	<key>COCKPIT_MOBILE_TOKEN</key>
	<string>COLE-AQUI-O-TOKEN</string>
</dict>
</plist>
